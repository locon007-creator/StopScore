# StopScore Android APK

This native Android wrapper opens the live StopScore Driver OS in a secure WebView.

## Automatic APK

Every change to `main` builds the APK and publishes it to the GitHub release named **StopScore Android**.

Direct download:

https://github.com/locon007-creator/StopScore/releases/latest/download/StopScore.apk
